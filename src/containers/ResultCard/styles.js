import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  resultsCard: {
    height: 300
  },
  resultsList: {
    maxHeight: 300
  },
  separator: {
    borderWidth: 1,
    borderColor: 'grey'
  }
})

export default styles
