import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  resultsList: {
    flex: 1
  },
  separator: {
    borderWidth: 1,
    borderColor: 'grey'
  }
})

export default styles
